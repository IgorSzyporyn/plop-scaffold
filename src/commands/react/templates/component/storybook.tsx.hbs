import React from 'react'
// also exported from '@storybook/react' if you can deal with breaking changes in 6.1
import { Story, Meta } from '@storybook/react/types-6-0'
import {{pascalCase name}}, { {{pascalCase name}}Type } from './{{pascalCase name}}'

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
} as Meta

{{#ifEqual "yes" withExamples}}
const Template: Story<{{pascalCase name}}Type> = (args) => <{{pascalCase name}} {...args} />

export const Basic = Template.bind({})
Basic.args = {
  title: 'Hello World'
}
{{else}}
const Template: Story<{{pascalCase name}}> = (args) => <{{pascalCase name}} {...args} />

export const Basic = Template.bind({})
Basic.args = {}
{{#ifEqual}}