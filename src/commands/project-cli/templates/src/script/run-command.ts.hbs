import { allowedCommands } from './index'

export function runCommand(command: Command) {
  const allowedCommand = allowedCommands.indexOf(command) > -1

  if (allowedCommand) {
    switch (command) {
      default:
        break
    }
  }
}
