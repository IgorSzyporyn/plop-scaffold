{{#ifEqual "yes" liftoff}}
import chalk from 'chalk'
import { print } from '../utils/print'
import { CONFIG_FILE_NAME } from '../constants'

export function helpMain() {
  print.title(`[--version] [--help]`)
  print(`              <command> [<args>]`)
  print.newline()
  print('List of commands')
  print.newline()
  print(
    `${chalk.blueBright(
      '  init'
    )}                        Initialize a ${CONFIG_FILE_NAME} config file in your folder`
  )
  print.newline()
  print(
    "'{{lowerCase name}} --help <command>' lists available arguments for the command."
  )
  print.newline()
}
{{else}}
import { print } from '../utils/print'

export function helpMain() {
  print.title(`[--version] [--help]`)
  {{#ifEqual "yes" commands}}
  print(`              <command> [<args>]`)
  print.newline()
  print('List of commands')
  print.newline()
    {{#ifEqual "yes" init}}
  print(
    `${chalk.blueBright(
      '  init'
    )}                        Initialize a ${CONFIG_FILE_NAME} config file in your folder`
  )
  print.newline()
    {{/ifEqual}}
  print(
    "'{{lowerCase name}} --help <command>' lists available arguments for the command."
  )
  print.newline()
  {{/ifEqual}}
}
{{/ifEqual}}

