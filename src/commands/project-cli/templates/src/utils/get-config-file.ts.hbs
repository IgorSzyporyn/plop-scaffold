import { getShared } from './shared'

export function getConfigFile(command: Command) {
  const { configPath } = getShared()
  let fileConfig = {}

  if (configPath) {
    const fileJson = require(configPath)

    if (fileJson && fileJson[command]) {
      fileConfig = fileJson[command]
    }
  }

  return fileConfig
}
