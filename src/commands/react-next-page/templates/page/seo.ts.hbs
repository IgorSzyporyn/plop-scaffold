import { NextSeoProps } from 'next-seo'
import { SEODefaults } from 'seo/seo-defaults'
import { config } from 'config'

export const {{camelCase name}}SEO: NextSeoProps = {
  title: 'Team Dixx',
  description: 'Average Gamers, Above Average Community',
  openGraph: {
    type: 'website',
    url: config.url,
    title: 'Team Dixx',
    description: 'Average Gamers, Above Average Community',
    // images should be overwritten in the component if a dynamic route
    // and changed to something more meaningful if normal page
    images: SEODefaults.openGraph.images,
  },
}
